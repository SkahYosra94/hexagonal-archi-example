server:
  port: 8080

logging:
  level:
    com.archi.hexagonal: debug

spring:
  application:
    name: hexagonal
  datasource:
    url: jdbc:postgresql://localhost:5432/hexagonal_project
    username: postgres
    password: yosra
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

